
<%= form_for(@billing_info) do |f| %>
  <% if @billing_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@billing_info.errors.count, "error") %> prohibited this billing_info from being saved:</h2>

      <ul>
      <% @billing_info.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br />
  <div class="field">
    <%= label :mode_of_collection, ' Select the Mode of Collection' %><br /><br />
    <%= text_field :mode_of_collection, params[:mode_of_collection] %>
  </div>
  <br />
  <div class="actions-single">
    <%= f.submit 'Continue for Receiving Details' %>
  </div>
  <br />
<% end %>
