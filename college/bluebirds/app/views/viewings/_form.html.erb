<%= form_for(@viewing) do |f| %>
  <% if @viewing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@viewing.errors.count, "error") %> prohibited this viewing from being saved:</h2>

      <ul>
      <% @viewing.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Movie_id %><br>
    <%= f.text_field :Movie_id %>
  </div>
  <div class="field">
    <%= f.label :AudiInfo_id %><br>
    <%= f.text_field :AudiInfo_id %>
  </div>
  <div class="field">
    <%= f.label :timing %><br>
    <%= f.time_select :timing %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
